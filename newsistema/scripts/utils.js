// Funções utilitárias extraídas de arquivos limpos
// function initBetaEvents() {
function removerLocal(chave) {
  localStorage.removeItem(chave);
}
function limparLocalStorage() {
  localStorage.clear();
}